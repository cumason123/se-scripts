public void Main(string argument, UpdateType updateSource)
{
    IMySensorBlock panel = (IMySensorBlock)GridTerminalSystem.GetBlockWithName("Detection.Sensor");
    MyDetectedEntityInfo entity = sensor.LastDetectedEntity();
    if (entity.Relationship === MyRelationsBetweenPlayerAndBlock.Enemies) {
        
    }
}
